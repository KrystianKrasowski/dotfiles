#!/usr/bin/env bash

# exit immediately on any error
set -e

# set xdg session variables
export XDG_CURRENT_DESKTOP=sway
export XDG_SESSION_DESKTOP=sway
export XDG_SESSION_TYPE=wayland

dbus-run-session sway
